{
    "description": "This JSON is a sample of useful variables available in template context.",
    "settings_env": "{{ settings_env }}",
    "settings.BASE_DIR": "{{ settings.BASE_DIR }}",
    "settings.STATIC_ROOT": "{{ settings.STATIC_ROOT }}",
    "settings.MEDIA_ROOT": "{{ settings.MEDIA_ROOT }}",
    "settings.DEPLOYMENT_APPNAME": "{{ settings.DEPLOYMENT_APPNAME }}",
    "settings.DEPLOYMENT_BUILD_DESTINATION": "{{ settings.DEPLOYMENT_BUILD_DESTINATION }}",
    "settings.DEPLOYMENT_HTTPSERVER_PORT": {{ settings.DEPLOYMENT_HTTPSERVER_PORT }},
    "settings.DEPLOYMENT_USER": "{{ settings.DEPLOYMENT_USER }}",
    "settings.DEPLOYMENT_GROUP": "{{ settings.DEPLOYMENT_GROUP }}",
    "SEARCH_INDEX": "{{ SEARCH_INDEX }}",
    "SOCKET_FILEPATH": "{{ SOCKET_FILEPATH }}",
    "APPSERVER_BINDING": "{{ APPSERVER_BINDING }}",
    "APPSERVER_BINDING_INTERFACE": "{{ APPSERVER_BINDING_INTERFACE }}"
}
